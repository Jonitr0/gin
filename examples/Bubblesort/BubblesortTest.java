import static org.junit.Assert.*;

public class BubblesortTest {
    @org.junit.Test
    public void testBubblesort() throws Exception {
        assertEquals(1,BubbleSort.bubbleSort());
        assertEquals(1,BubbleSort.bubbleSort());
        assertEquals(1,BubbleSort.bubbleSort());
        assertEquals(1,BubbleSort.bubbleSort());
    }
}
